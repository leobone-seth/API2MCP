# API2MCP 项目说明 <blockquote/>
## 1. 用了什么技术
后端 / MCP 部分

- Python 3
- FastAPI ：管理后台 HTTP API（命名空间、接口配置、MCP 启停等）
  - 代码： app.py
- FastMCP ：MCP Server 框架，用来把 Python 函数暴露为 MCP 工具
  - 代码： server.py
- httpx ：在 MCP 工具中发起真正的 HTTP 请求
- subprocess + threading ：后台拉起 MCP 子进程、管理日志输出
- Pydantic ：定义 API 规格、命名空间等数据模型（FastAPI 请求体）
- dotenv ：读取 .env 配置（如 MCP_NAMESPACE 等）
- JSON + 本地文件存储 ：
  - 所有接口与命名空间配置统一存储到本地 apis.json
  - 代码中通过 _load_config/_save_config 读写
前端

- React 18/19 ：构建单页应用控制台
  - 入口： frontend/App.tsx
- TypeScript ：类型定义（如 APISpec , NamespaceMeta 等）
  - frontend/types.ts
- Vite ：开发与构建工具
  - frontend/vite.config.ts
- Tailwind 风格 Utility class（纯 className，不用 Tailwind 工具链） ：快速布局和样式
- Recharts ：Dashboard 图表展示（调用次数等占位）
其他

- Node.js ：前端依赖安装、打包
- 浏览器原生 Fetch ：前端调用 FastAPI 管理接口
  - frontend/services/apiService.ts
## 2. 具体用什么实现了哪些功能
### 2.1 MCP 工具自动生成与转发（核心）
- 技术组合 ：FastMCP + httpx + JSON 配置
- 实现文件 ： server.py
关键点：

1. 读取 apis.json 并校验
   
   - 函数： _load_apis_json
   - 支持两种结构：
     - 顶层数组： [{...}, {...}]
     - 顶层对象： {"apis": [ ... ]}
   - 校验字段：
     - name 、 method 必须存在且非空
     - 必须有 url 或 base_url + path
     - enabled 为假时跳过该接口
2. 动态注册 MCP 工具
   
   - 函数： _register_api_tools
   - 对每个 spec 创建一个异步 tool 函数 _tool(request)
   - 通过 mcp.tool(name=tool_name, description=tool_desc)(_tool) 注册到 FastMCP
   - tool 内部调用 _call_http ，统一发 HTTP 请求并返回结构化结果
3. HTTP 调用封装
   
   - 函数： _call_http
   - 把 spec 和调用时传入的 request 合并：
     - default_headers + request.headers
     - default_query + request.query
     - default_json / default_data 与 request.json / request.data
     - timeout_s 优先用 request，否则用 spec，默认 30 秒
   - 使用 httpx.AsyncClient 发请求，并返回统一结构：
     ```
     {
       "ok": true,
       "status_code": 200,
       "url": "...",
       "headers": { ... },
       "body": ...  // JSON 或文本
     }
     ```
4. 命名空间过滤
   
   - 通过环境变量 MCP_NAMESPACE 决定当前 MCP 进程只加载哪个命名空间的 API
   - target_namespace != "all" 时，会按 spec["namespace"] 过滤
### 2.2 管理后台 API（命名空间 + 接口配置 + MCP 进程）
- 技术组合 ：FastAPI + Pydantic + subprocess + 本地 JSON 文件
- 实现文件 ： app.py
配置存储层

- _load_config / _save_config ：
  - 读写 apis.json ，统一结构：
    ```
    {
      "namespaces": [...],
      "apis": [...]
    }
    ```
- _load_local_apis / _save_local_apis ：
  - 对 apis 子结构做增删改
- _load_namespaces_from_json / _save_namespaces_to_json ：
  - 对 namespaces 子结构做增删改 2.2.1 命名空间管理
- 数据模型 ： class Namespace(BaseModel)
  
  - 字段： name , version , prot （端口号）
- 接口：
  
  - GET /api/namespaces
    - 汇总 namespaces 和 apis 中出现过的 namespace
    - 保证至少有一个 default
  - POST /api/namespaces
    - 新增命名空间（校验重名与端口冲突）
  - PUT /api/namespaces/{name}
    - 修改名称、版本、端口
    - 同步更新 apis 中该 namespace
    - 如果 MCP 进程已在运行，还会迁移 mcp_processes 的 key
  - DELETE /api/namespaces/{name}
    - 停止对应 MCP 子进程
    - 删除 namespaces 及该 namespace 下的所有 API
端口号逻辑

- prot 为正整数则视为指定端口；否则视为「自动分配」
- 启动 MCP 时先尝试命名空间的 prot ，如冲突则自动递增寻找可用端口 2.2.2 API 接口管理（APISpec）
- 数据模型 ： class ApiSpec(BaseModel)
  
  - 重点字段：
    - name , method , url / base_url+path
    - description
    - default_headers , default_query , default_json , default_data , default_response
    - timeout_s
    - enabled
    - namespace
- 接口：
  
  - GET /api/apis?namespace=xxx
    - 从本地 apis.json 中加载对应 namespace 的 API
    - 若 namespace=all 则返回全部
    - 补充 id （默认为 name ）
  - POST /api/apis
    - 新增 API（直接 append 到 apis 列表）
  - PUT /api/apis/{api_id}
    - 以 name == api_id 为 key 做更新
  - DELETE /api/apis/{api_id}
    - 删除 name == api_id 的记录 2.2.3 MCP 子进程管理与代理
- 数据结构 ： mcp_processes: Dict[str, {process, port, output, transport}]
启动 MCP

- POST /api/start?namespace=xxx
  - 如果 namespace 为空，则扫描：
    - 所有启用的 apis 的 namespace
    - 所有 namespaces 中定义的 name <br/> -> 全量启动
  - 单个 namespace 由 start_single_mcp(namespace) 完成：
    - 选择端口（优先命名空间 prot ，否则从 8020 起自动占用）
    - 构造环境变量：
      - MCP_NAMESPACE=xxx
      - STORAGE_MODE=local
    - subprocess.Popen 启动 python server.py --port {port} --transport {transport}
    - 后台线程 enqueue_output 读取 stdout，写入 log 文件和内存中的 output 列表
停止 MCP

- POST /api/stop?namespace=xxx
  - 若无 namespace 参数，则停止所有 MCP 子进程
  - 调用 .terminate() 并从 mcp_processes 删除
代理 MCP HTTP/SSE

- @app.api_route("/mcp_{namespace}", ...)
- @app.api_route("/mcp_{namespace}/{path:path}", ...)
逻辑：

1. 检查目标 namespace 是否在 mcp_processes 中运行
2. 根据请求是否为 SSE：
   - 如果是 SSE，则转发到子进程 /sse
   - 否则转发到 /mcp
3. 复制原始请求 headers/body 并移除 host 等敏感头
4. 使用 httpx.AsyncClient 转发请求并流式返回
状态与日志

- GET /api/mcp_info
  - 返回所有正在运行的 MCP 实例：
    - namespace
    - url （根据 transport 选择 /mcp 或 /sse ）
    - tools （从本地 apis 中找该 namespace 下 enabled 的 tool 名称）
- GET /api/status
  - 返回是否有正在运行的 MCP 进程，以及数量
- GET /api/logs?namespace=xxx
  - 返回该 namespace 的 stdout 日志列表
### 2.3 前端控制台功能
- 技术组合 ：React + TypeScript + Vite + Fetch + Utility CSS
- 主文件：
  - frontend/App.tsx
  - frontend/components/EndpointList.tsx
  - frontend/components/ServerStatusView.tsx
  - frontend/services/apiService.ts
主要功能模块

1. 命名空间工作区卡片
   
   - 顶部水平卡片列表，展示所有命名空间
   - 每个卡片显示：
     - 名称
     - 版本 V1.0.0
     - 端口号 Port 8020 / 自动分配
   - 支持：
     - 点击切换当前工作空间（影响下方接口列表）
     - hover 显示编辑、删除按钮
     - 点击「添加命名空间」打开表单（名称/版本/端口）
2. 接口列表与编辑弹窗
   
   - 列出当前 namespace 的所有 API：
     - HTTP 方法 badge
     - 工具名称
     - 命名空间
     - URL/路径
     - 启用状态
   - 支持：
     - 搜索（按名称、路径、命名空间模糊）
     - 新增 / 编辑 / 删除接口
   编辑弹窗内容（EndpointList.tsx）：
   
   - 基本信息：
     - 工具名称、命名空间、描述
     - HTTP 方法、超时时间
     - 完整 URL 与「解析」按钮（分解为 base_url + path）
   - 请求地址：
     - base_url
     - path
   - 默认请求配置：
     - 默认请求头（JSON）
     - 默认请求体（JSON）
     - 默认响应体（JSON）
     - 每块旁边都有一个 “格式化” 按钮：
       - 去掉 // 行尾注释
       - 自动补双引号： key: value → "key": value
       - 统一格式化成缩进良好的 JSON
   - 启用开关：是否启用该接口
   保存时：
   
   - 对三个 JSON 字段做严格解析，错误时显示中文提示并阻止保存
   - 通过 apiService.addApi / updateApi 调后端写入 apis.json
3. MCP 服务器状态页（ServerStatusView）
   
   - 显示：
     - 是否有 MCP 实例在运行（总数）
     - 当前 namespace 是否已运行
   - 可操作：
     - 启动当前命名空间
     - 启动所有命名空间
     - 停止所有
     - 针对每个正在运行的 namespace：
       - 显示 MCP 服务地址
       - 展示工具列表
       - 单独停止该 namespace
4. JSON 辅助工具
   
   - 请求头 / 请求体 / 响应体 统一 JSON 格式输入区
   - 增强的格式化按钮支持“宽松输入”，用户可以用接近 JS 对象的风格书写，由前端自动转换为合法 JSON
## 3. 各个功能做了什么、实现了什么
可以理解成三层：

1. 配置层 （ apis.json ）
   
   - 把所有「接口定义」抽象为结构化 JSON，统一描述：
     - 命名空间、工具名、描述
     - 请求方法、路径、默认参数
   - 实现效果 ： <br/> 不需要再写 Python 代码就能扩展新的 MCP 工具，只要编辑 JSON / 前端表单即可。
2. 运行层（MCP server + 管理后台）
   
   - MCP server（ server.py ）负责：
     - 读取配置并动态注册 MCP tools
     - 把 MCP 调用转成真实 HTTP 请求
     - 返回统一结构的响应（方便在 LLM / MCP 客户端里消费）
   - 管理后台（ app.py ）负责：
     - CRUD 命名空间和接口定义（写入 apis.json ）
     - 启动 / 停止不同命名空间的 MCP 子进程
     - 代理 MCP HTTP/SSE 请求
     - 提供运行状态和日志
   实现效果 ： <br/> 一键启停不同“工作空间”的 MCP 服务，每个工作空间对应一批 HTTP 工具。
3. 交互层（前端 React 控制台）
   
   - 提供一个 Notion 风格的 GUI，让你：
     - 像管理配置中心一样管理 namespace 和 API
     - 直观看到哪些 MCP 工具在跑、跑在哪个端口
     - 更方便地输入和格式化 JSON 请求体 / 响应体样例
   实现效果 ： <br/> 你可以完全通过 Web 页面对接 ERP/内部系统的 HTTP 接口，然后一键在 Trae 等 MCP 客户端中使用这些接口。
视为一个完整的 markdown 文档。）